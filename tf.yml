name: tf
# conda env create -f requirements.yml
# then conda activate tf
# to remove, simply do conda env remove -n tf
channels:
- anaconda # default one
- tensorflow # necessary for tensorflow models
dependencies:
- python=3.11.6
- numpy=1.26.0
- pip # needed to install pip dependencies below
- tensorflow==2.13.1
- scipy=1.1.0
- pip:
  - scikit-learn==1.3.2
  #- opencv-contrib-python==4.0.0.21
